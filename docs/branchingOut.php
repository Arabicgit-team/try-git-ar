<div id="doc-template">
    <h1>
        <em>18.</em> 
        <?php echo _("Branching Out"); ?>
    </h1>
    <div id="challenge-descroption">
        <p>
            <?php echo _("When developers are working on a feature or bug 
            they'll often create a copy (aka. branch) of their code they can 
            make separate commits to. Then when they're done they can merge 
            this branch back into their main master branch."); ?>
        </p>
        <p>
            <?php  echo _("We want to remove all these pesky octocats, so let's 
            create a branch called clean_up, where we'll do all the work:"); ?>
        </p>
        <p>
            <code dir="ltr" class="input-command">
                git branch clean_up
            </code>
        </p>
    </div>
</div>


