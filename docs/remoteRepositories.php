<div id="doc-template">
    <h1>
        <em>10.</em> 
        <?php echo _("Remote Repositories"); ?>
    </h1>
    <div id="challenge-descroption">
        <p>
            <?php echo _("Great job! We've gone ahead and created a new empty 
            GitHub repository for you to use with Try Git at 
            https://github.com/try-git/try_git.git. To push our local repo to 
            the GitHub server we'll need to add a remote repository."); ?>
        </p>
        <p>
            <?php  echo _("This command takes a remote name and a repository 
            URL, which in your case is https://github.com/try-git/try_git.git.
           <br /> Go ahead and run git remote add with the options below:"); ?>
        </p>
        <p>
            <code dir="ltr" class="input-command">
                git remote add origin https://github.com/try-git/try_git.git
            </code>
        </p>
    </div>
</div>
