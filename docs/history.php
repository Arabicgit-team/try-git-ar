<div id="doc-template">
    <h1>
        <em>9.</em> 
        <?php echo _("History"); ?>
    </h1>
    <div id="challenge-descroption">
        <p>
            <?php echo _("So we've made a few commits. Now let's browse them 
            to see what we changed."); ?>
        </p>
        <p>
            <?php echo _("Fortunately for us, there's git log. Think of Git's 
            log as a journal that remembers all the changes we've committed so 
            far, in the order we committed them. Try running it now:"); ?>
        </p>
        <p>
            <code class="input-command">git log</code>
        </p>
    </div>
</div>
